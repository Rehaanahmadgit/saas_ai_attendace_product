DATABASE_URL=postgresql://neondb_owner:npg_fEYkR3WdJD8v@ep-hidden-shape-anpp2edr-pooler.c-6.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# JWT Secrets — CHANGE BOTH before deploying to production
# Generate with: python -c "import secrets; print(secrets.token_hex(64))"
JWT_SECRET_KEY=nexus-saas-attendance-super-secret-key-change-before-deploy
JWT_REFRESH_SECRET_KEY=nexus-saas-refresh-token-separate-secret-change-before-deploy

ACCESS_TOKEN_EXPIRE_HOURS=24

APP_ENV=development

# CORS — comma separated allowed origins
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Legacy SHA-256 password compat — set to 1 only during migration, then remove
# LEGACY_SHA256_PASSWORDS=0
